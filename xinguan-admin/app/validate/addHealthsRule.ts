export default {
  healthsName: {
    type: 'string',
    trim: true,
    allowNull: false,
  },
  healthsSex: {
    type: 'boolean',
    allowNull: false,
  },
  healthsJob: {
    type: 'string',
    trim: true,
    allowNull: false,
  },
  healthsHeat: {
    type: 'string',
    trim: true,
    allowNull: false,
  },
  healthsState: {
    type: 'boolean',
    allowNull: false,
  },
};
