export default {
  goodsInName: {
    type: 'string',
    trim: true,
    allowNull: false,
  },
  goodsInType: {
    type: 'string',
    trim: true,
    allowNull: false,
  },
  goodsInTotal: {
    type: 'number',
    trim: true,
    allowNull: false,
  },
  goodsInUnit: {
    type: 'string',
    trim: true,
    allowNull: false,
  },
  goodsInUsername: {
    type: 'string',
    trim: true,
    allowNull: false,
  },
  goodsInJob: {
    type: 'string',
    trim: true,
    allowNull: false,
  },
  goodsInDate: {
    type: 'string',
    trim: true,
    allowNull: false,
  },
};
